fastapi==0.116.1
uvicorn[standard]==0.30.6

# SQLAlchemy y SQL Server
sqlalchemy>=2.0.0
pyodbc>=4.0.0  # Driver ODBC para SQL Server

alembic>=1.12.0  # Para migraciones

# Autenticación JWT
python-jose[cryptography]>=3.3.0
pyjwt>=2.8.0  # JWT handling directo
passlib[bcrypt]>=1.7.4  # Versión más reciente para compatibilidad
bcrypt>=4.0.0,<5.0.0  # Versión compatible con passlib
python-multipart>=0.0.6
email-validator>=2.0.0

# LangChain (núcleo + integraciones)
langchain==0.3.27
langchain-milvus==0.2.1
langchain-ollama>=0.1.0
langchain-community>=0.3.0  # Para chains adicionales
langchain-core>=0.3.0      # Core components

# Embeddings
sentence-transformers>=2.2.0  # Para generar embeddings de texto

# Ollama integration
httpx>=0.24.0

# Procesamiento de documentos y audio para ingesta
tika>=3.1.0  # Apache Tika para extraer texto de documentos (última versión)
chardet>=5.0.0  # Detección automática de codificación de caracteres
filetype>=1.2.0  # Detección de tipos de archivo pura Python (alternativa a python-magic)
faster-whisper>=0.10.0  # faster-whisper para transcripción de audio optimizada
pydub>=0.25.1  # Manipulación de archivos de audio
ffmpeg-python>=0.2.0  # FFmpeg wrapper para conversión de audio

# OCR para documentos escaneados (fallback cuando Tika no puede extraer texto)
easyocr>=1.7.0  # OCR multiplataforma para imágenes y documentos escaneados
pymupdf>=1.23.0  # Conversión de PDF a imágenes para OCR (multiplataforma, solo pip)

# Milvus SDK (2.5.x o superior)
protobuf>=5.27.2,<6.0.0
pymilvus>=2.5.14,<2.7

# Compatibilidad para evitar conflictos numpy/pandas
numpy>=1.24.0,<2.0.0
pandas>=2.0.0

python-dotenv==1.0.1

# Monitoreo del sistema
psutil>=5.9.0        # Para detectar recursos del sistema (CPU, RAM, etc.)

# Tareas asíncronas y cola de trabajo
celery>=5.3.0
redis>=5.0.0

# Envío de correos electrónicos
fastapi-mail>=1.4.1  # Biblioteca para envío de correos con FastAPI
aiosmtplib>=3.0.0    # Cliente SMTP asíncrono
jinja2>=3.1.0        # Para plantillas de correo