# ================================
# CONFIGURACIÓN GENERAL DEL SISTEMA
# ================================

# Entorno de aplicación (development o production)
ENVIRONMENT=development

# ================================
# BASE DE DATOS Y ALMACENAMIENTO
# ================================

# Configuración SQL Server Azure
SQL_SERVER_HOST=servidor.database.windows.net
SQL_SERVER_PORT=1433
SQL_SERVER_DATABASE=db_JusticIA
SQL_SERVER_USER=usuario_sql
SQL_SERVER_PASSWORD=password_seguro
SQL_SERVER_DRIVER=ODBC Driver 18 for SQL Server

# ================================
# VECTORSTORE Y EMBEDDINGS
# ================================

# Configuración Milvus (Base de datos vectorial)
# Opción 1: Zilliz Cloud (Recomendado para producción)
MILVUS_URI=https://xxx.serverless.aws-region.cloud.zilliz.com
MILVUS_TOKEN=token_zilliz
# Opción 2: Milvus Local
# MILVUS_URI=http://localhost:19530
# MILVUS_TOKEN=

MILVUS_DB_NAME=JusticIA
COLLECTION_NAME=justicia_docs

# Configuración de embeddings para búsqueda semántica
EMBEDDING_MODEL=Dariolopez/bge-m3-es-legal-tmp-6
DIM=1024

# ================================
# MODELOS LLM E INTELIGENCIA ARTIFICIAL
# ================================

# Configuración Ollama (Servidor LLM)
# Opción 1: Ollama Cloud
OLLAMA_BASE_URL=https://ollama.com
OLLAMA_MODEL=gpt-oss:20b-cloud
OLLAMA_API_KEY=api_key_ollama
# Opción 2: Ollama Local
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama2:7b
# OLLAMA_API_KEY=

# Configuración LLM Principal - Parámetros optimizados
LLM_TEMPERATURE=0.3
LLM_KEEP_ALIVE=15m
LLM_REQUEST_TIMEOUT=600
LLM_NUM_CTX=32768
LLM_NUM_PREDICT=4096
LLM_TOP_K=50
LLM_TOP_P=0.88
LLM_REPEAT_PENALTY=1.0

# ================================
# SERVICIOS DE PROCESAMIENTO
# ================================

# URL de Redis para Celery (broker de mensajes y backend de resultados)
# En Docker Compose se sobrescribe automáticamente a redis://redis:6379
REDIS_URL=redis://localhost:6379

# URL del servidor Apache Tika para extracción de texto y OCR
# En Docker Compose se sobrescribe automáticamente a http://tika:9998
TIKA_SERVER_URL=http://localhost:9998

# Timeout para procesamiento de archivos con Tika (en segundos)
TIKA_TIMEOUT=900

# ================================
# PROCESAMIENTO DE AUDIO
# ================================

# Configuración faster-whisper para transcripción de audio
# Opciones: tiny, base, small, medium, large, large-v2, large-v3
# Desarrollo: base, Producción: medium o large
WHISPER_MODEL=base

# ================================
# AUTENTICACIÓN Y SEGURIDAD
# ================================

# Configuración JWT
# IMPORTANTE: Cambiar en producción (generar con: openssl rand -hex 32)
SECRET_KEY=cambiar-en-produccion-generar-con-openssl
JWT_EXPIRE_HOURS=8
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Usuario Administrador Inicial (creado automáticamente en migración)
# CAMBIAR ESTOS VALORES EN PRODUCCIÓN
ADMIN_CEDULA=000000000
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@dominio.com
ADMIN_PASSWORD=CambiarEnProduccion2025!
ADMIN_NOMBRE=Administrador
ADMIN_APELLIDO=Sistema

# ================================
# CONFIGURACIÓN DE EMAIL
# ================================

# Configuración de Email
EMAIL_PROVIDER=gmail
EMAIL_USERNAME=email@gmail.com
EMAIL_PASSWORD=app-password-generado

# Para Gmail:
# 1. Acceder a https://myaccount.google.com/security
# 2. Activar verificación en 2 pasos
# 3. Generar una "contraseña de aplicación"
# 4. Utilizar esa contraseña aquí (no la contraseña normal)

# Otros proveedores disponibles:
# EMAIL_PROVIDER=outlook    # para Outlook/Hotmail
# EMAIL_PROVIDER=yahoo      # para Yahoo
# EMAIL_PROVIDER=zoho       # para Zoho
# EMAIL_PROVIDER=custom     # para otros proveedores
